language: node_js
node_js:
  - stable
cache:
  directories:
    - node_modules
script:
  - npm run build
deploy:
  provider: heroku
app: spacex-launch-programs
true:
  repo: gurpreetbirdi/spacex-launch-programs
  branch: master
